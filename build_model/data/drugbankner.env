# DrugBank credentials (used for HTTP basic auth download)
DRUGBANK_EMAIL="your@email"
DRUGBANK_PASSWORD="yourpassword"

# Optional: where to clone + run
WORKDIR="$HOME/work/drugbankner_run"

KG_VERSION="2.10.2"
